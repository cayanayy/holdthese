version: '3.9'

services:
  api:
    ports:
      - "8080:8080"
    build: .
    depends_on:
      - database
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://database:5432/holdthese
  database:
    image: postgres:alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: topsecret
      POSTGRES_DB: holdthese